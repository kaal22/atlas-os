# Calamares finished page — reboot after install
---
restartNowEnabled: true
restartNowChecked: true
# -i: allow interactive inhibitors (same as Debian calamares-settings)
restartNowCommand: "systemctl -i reboot"
