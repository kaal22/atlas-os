# Arcalium OS GRUB identity (overrides Debian defaults from live-build)
GRUB_DISTRIBUTOR="Arcalium"
# Keep quiet boot readable; full custom Plymouth theme is a later polish pass.
GRUB_CMDLINE_LINUX_DEFAULT="quiet"
